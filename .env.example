# SafePaste API — Environment Variables
# Copy this file to .env and fill in your values.
# The defaults below work for local development; override them in production.

# Port the API server listens on (Railway sets this automatically)
PORT=3000

# API keys — replace with strong, secret values in production.
# Generate new keys with: node -e "console.log('sp_' + require('crypto').randomBytes(24).toString('base64url'))"
SAFEPASTE_DEMO_KEY=sp_demo_key_12345
SAFEPASTE_PRO_KEY=sp_pro_key_67890
