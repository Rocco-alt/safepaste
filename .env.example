# SafePaste API — Environment Variables
# Copy this file to .env and fill in your values.
# The defaults below work for local development; override them in production.

# Port the API server listens on (Railway sets this automatically)
PORT=3000

# API keys — replace with strong, secret values in production.
# Generate new keys with: node -e "console.log('sp_' + require('crypto').randomBytes(24).toString('base64url'))"
SAFEPASTE_DEMO_KEY=sp_demo_key_12345
SAFEPASTE_PRO_KEY=sp_pro_key_67890

# PostgreSQL connection string (Railway sets this automatically when you add the Postgres addon)
# For local development, leave commented out to use in-memory keys instead.
# DATABASE_URL=postgresql://user:password@localhost:5432/safepaste

# Admin key for managing API keys (POST /v1/keys, DELETE /v1/keys/:id)
# Generate with: node -e "console.log('sk_admin_' + require('crypto').randomBytes(24).toString('base64url'))"
SAFEPASTE_ADMIN_KEY=sk_admin_dev_12345
